{
  "name": "@gramps-connect/gramps-date",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript port of Gramps' Date model, calendar conversion, and locale-aware date display -- for clients (like layer2-local-cache) that receive raw Gramps Date structs from gramps-web-api and need to render/build them without a slow per-object round trip through Gramps' own Python date displayer.",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "tsx --test src/__tests__/*.test.ts"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "tsx": "^4.23.5",
    "@types/node": "^26.1.2"
  }
}
